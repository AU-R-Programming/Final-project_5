% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confusion_matrix.R
\name{confusion_matrix_metrics}
\alias{confusion_matrix_metrics}
\title{Calculate Confusion Matrix and Metrics}
\usage{
confusion_matrix_metrics(y_true, y_pred)
}
\arguments{
\item{y_true}{Numeric vector of true labels (0 or 1).}

\item{y_pred}{Numeric vector of predicted labels (0 or 1).}
}
\value{
A list containing prevalence, accuracy, sensitivity, specificity, false discovery rate, and diagnostic odds ratio.
}
\description{
This function calculates the confusion matrix and other performance metrics for logistic regression predictions.
}
\examples{
n <- 100
X <- cbind(1, matrix(rnorm(n * 2), ncol = 2))
y <- rbinom(n, 1, prob = 0.5)
beta_est <- estimate_beta(X, y)
y_pred <- predict(beta_est, X)
metrics <- confusion_matrix_metrics(y, y_pred)
print(metrics)
}
