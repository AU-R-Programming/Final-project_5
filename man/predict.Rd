% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_labels.R
\name{predict}
\alias{predict}
\title{Predict Labels for Logistic Regression}
\usage{
predict(beta, X, cutoff = 0.5)
}
\arguments{
\item{beta}{Numeric vector of coefficients.}

\item{X}{Numeric matrix of predictors.}

\item{cutoff}{Cutoff value to classify probabilities as 0 or 1 (default is 0.5).}
}
\value{
A numeric vector of predicted labels (0 or 1).
}
\description{
This function predicts binary labels for each observation given the beta coefficients, predictors, and a cutoff value.
}
\examples{
n <- 100
X <- cbind(1, matrix(rnorm(n * 2), ncol = 2))
y <- rbinom(n, 1, prob = 0.5)
beta_est <- estimate_beta(X, y)
y_pred <- predictlabels(beta_est, X)
print(y_pred)
}
